version: '3.6'

services:
  serveur1:
    build:
      context: ./api/serveur1
    ports:
      - 8080:8080
    expose:
      - 8080

  client:
    build:
      context: ./app
    ports:
      - 3000:3000
    expose:
      - 3000